#include <stdio.h>

int main()
{
    int c,n,i;
    char a[10];
    scanf("%d",&n);
    scanf("%s",&a[i]);
    for(i=0;i<=n;i++)
    {
    if(a[i]>='a'&&a[i]<='z'||a[i]>='A'&&a[i]<='Z')
    {
        printf("%d",a[i]);
    }
    }

    return 0;
}
